import React from "react"
import ReactDOM from "react-dom"
import App from "./App"

// no need for a function here since App is already imported, you can just directly use it in ReactDOM.render()

ReactDOM.render(<App />, document.getElementById("root"))
